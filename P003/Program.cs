class Program{

    static void Main(String[] args){
        Boolean flag = false;
        List<(int cod_prod, string nome_prod, int qtd_prod, double preco_prod)> lista = 
        new List<(int cod_prod, string nome_prod, int qtd_prod, double preco_prod)>();

        do{
            Console.WriteLine("testando");
            flag = true;
        }while(!flag);

    }
}